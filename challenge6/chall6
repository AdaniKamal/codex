┌──(kali㉿adanikamal)-[~/chall]
└─$ codex --yolo "break the challenge6.txt file and find the flag"
╭─────────────────────────────────────────────────╮
│ ✨ Update available! 0.113.0 -> 0.130.0         │
│ Run npm install -g @openai/codex to update.     │
│                                                 │
│ See full release notes:                         │
│ https://github.com/openai/codex/releases/latest │
╰─────────────────────────────────────────────────╯

╭───────────────────────────────────────────────────╮
│ >_ OpenAI Codex (v0.113.0)                        │
│                                                   │
│ model:     gpt-5.3-codex xhigh   /model to change │
│ directory: ~/chall                                │
╰───────────────────────────────────────────────────╯

  Tip: New Use /fast to enable our fastest inference at 2X plan usage.


› break the challenge6.txt file and find the flag


• I’ll inspect challenge6.txt and nearby files to identify the cipher/encoding, then decode it and extract the flag. I’m starting by listing the directory and previewing the file contents.

• Explored
  └ List ls -la
    List rg --files
    Read challenge6.txt

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• I found the file is a long GNU Manifesto passage with heavily mixed uppercase/lowercase, which is likely the steganographic channel. Next I’ll test decodings from the capitalization pattern (binary,
  Bacon, and grouped schemes).

• Ran wc -l challenge6.txt && wc -c challenge6.txt
  └ 26 challenge6.txt
    4382 challenge6.txt

• Ran tr -cd '[:alpha:]' < challenge6.txt | wc -c
  └ 3440

• Ran perl -ne 'while(/([A-Za-z])/g){$c=$1; print(($c=~/[A-Z]/)?1:0)}' challenge6.txt | fold -w 8 | head -n 40
  └ 01010101
    01000101
    … +36 lines
    01110111
    01000001

• Ran perl -0777 -ne '$s=$_; $bits=""; while($s=~/([A-Za-z])/g){$bits.=($1 ge "A" && $1 le "Z")?1:0;} for($i=0;$i+7<length($bits);$i+=8){print chr(oct("0b".substr($bits,$i,8)));}' challenge6.txt | sed -n
  │ '1,40p'
  └ UEsDBBQACQAIAAuMq1zgO1MCiAAAAH4AAAAIABwAZmxhZy50eHRVVAkAAwWGAWoGhgFqdXgLAAEE
    6AMAAAToAwAA6E+MQBxcldO53WvkVzCUo+23eMdP6SE3g/u/s0rAh2q5oV9H6beZ3pg4O3E0ev8L
    … +2 lines
    CAALjKtc4DtTAogAAAB+AAAACAAYAAAAAAABAAAA/4EAAAAAZmxhZy50eHRVVAUAAwWGAWp1eAsA
    AQToAwAABOgDAABQSwUGAAAAAAEAAQBOAAAA2gAAAAAA

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• The capitalization decodes cleanly into Base64 that starts with UEsDB... (a ZIP archive). I’ll decode it to a zip file and extract flag.txt now.

• Ran perl -0777 -ne '$s=$_; $bits=""; while($s=~/([A-Za-z])/g){$bits.=($1 ge "A" && $1 le "Z")?1:0;} for($i=0;$i+7<length($bits);$i+=8){print chr(oct("0b".substr($bits,$i,8)));}' challenge6.txt >
  │ hidden.b64 && base64 -d hidden.b64 > hidden.zip && file hidden.zip && unzip -l hidden.zip
  └ hidden.zip: Zip archive data, made by v3.0 UNIX, extract using at least v2.0, last modified May 11 2026 17:32:22, uncompressed size 126, method=deflate
    Archive:  hidden.zip
    … +3 lines
    ---------                     -------
          126                     1 file

• Ran unzip -p hidden.zip flag.txt
  └    skipping: flag.txt                unable to get password

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• The ZIP is password-protected, so I’ll recover the password next (likely from another hidden channel or by cracking the ZIP hash). I’m checking available cracking tools and extracting the hash.

• Ran which zip2john || true; which john || true; which fcrackzip || true; which 7z || true
  └ /usr/sbin/zip2john
    /usr/sbin/john
    /usr/bin/fcrackzip
    /usr/bin/7z

• Ran 7z l -slt hidden.zip 2>/dev/null | sed -n '1,200p'
  └
    7-Zip 25.01 (x64) : Copyright (c) 1999-2025 Igor Pavlov : 2025-08-03
    … +29 lines
    Offset = 0


• Ran zip2john hidden.zip > hidden.hash && cat hidden.hash
  └ ver 2.0 efh 5455 efh 7875 hidden.zip/flag.txt PKZIP Encr: TS_chk, cmplen=136, decmplen=126, crc=02533BE0 ts=8C0B cs=8c0b type=8
    hidden.zip/flag.txt:$pkzip$1*1*2*0*88*7e*2533be0*0*42*8*88*8c0b**$/pkzip$:flag.txt:hidden.zip::hidden.zip

• Ran ls -lh /usr/share/wordlists 2>/dev/null | sed -n '1,120p'
  └ total 185M
    lrwxrwxrwx 1 root root   26 Nov 25 08:36 amass -> /usr/share/amass/wordlists
    … +12 lines
    lrwxrwxrwx 1 root root   25 Nov 25 08:36 wfuzz -> /usr/share/wfuzz/wordlist
    lrwxrwxrwx 1 root root   37 Nov 25 08:36 wifite.txt -> /usr/share/dict/wordlist-probable.txt

• Ran john --show hidden.hash 2>/dev/null || true
  └ 0 password hashes cracked, 1 left

• Ran john --wordlist=/usr/share/wordlists/rockyou.txt hidden.hash
  └ Using default input encoding: UTF-8
    Loaded 1 password hash (PKZIP [32/64])
    … +4 lines
    Use the "--show" option to display all of the cracked passwords reliably
    Session completed.

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• Password recovered (gandalf). I’ll decrypt flag.txt and return the flag.

• Ran unzip -P gandalf -p hidden.zip flag.txt
  └ Congratulations! Here is your first flag:

    … +2 lines
    Continue here:
    https://chortle.0hl.cc

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• No listed SKILL.md skill applied here; this was a direct stego/crypto crack.

  Flag found:

  PRJBLK{1/6:_U_@R3_0FF_2_A_f1YInG_$t@rT}

  I extracted it by decoding letter case in challenge6.txt into Base64, recovering an encrypted ZIP, cracking ZIP password gandalf, then reading flag.txt.
